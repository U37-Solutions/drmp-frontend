name: Build

inputs:
  DOCKER_REGISTRY_NAME:
    type: string
    required: true
  NAMESPACE:
    type: string
    required: true
  PROJECT_NAME:
    type: string
    required: true
  NEXT_PUBLIC_API_URL:
    type: string
    required: true
  NEXT_PUBLIC_WS_URL:
    type: string
    required: true
  NEXT_PUBLIC_MAP_API_KEY:
    type: string
    required: true
  NEXT_PUBLIC_LIGHT_MAP_ID:
    type: string
    required: true
  NEXT_PUBLIC_DARK_MAP_ID:
    type: string
    required: true

runs:
  using: 'composite'
  steps:
    - name: Build and push Docker image
      uses: docker/build-push-action@v3
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: |
          ${{ inputs.DOCKER_REGISTRY_NAME }}/${{ inputs.NAMESPACE }}/${{ inputs.PROJECT_NAME }}:latest
        platforms: linux/amd64
        secrets: |
          NEXT_PUBLIC_MAP_API_KEY=${{ inputs.NEXT_PUBLIC_MAP_API_KEY }}
          NEXT_PUBLIC_LIGHT_MAP_ID=${{ inputs.NEXT_PUBLIC_LIGHT_MAP_ID }}
          NEXT_PUBLIC_DARK_MAP_ID=${{ inputs.NEXT_PUBLIC_DARK_MAP_ID }}
        build-args: |
          NEXT_PUBLIC_API_URL=${{ inputs.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_WS_URL=${{ inputs.NEXT_PUBLIC_WS_URL }}
